output_path             = "output_path"
output_size             = 1024
backpressure            = 10

sandbox_load_path       = "load"
sandbox_run_path        = "run"
io_lua_path             = "io/?.lua"
io_lua_cpath            = "io/?.so"
analysis_lua_path       = "analysis/?.lua"
analysis_lua_cpath      = "analysis/?.so"

input_defaults = {
    output_limit        = 1023,
    memory_limit        = 32767,
    instruction_limit   = 1000,
    preserve_data       = true,
}

analysis_defaults = {
}

output_defaults = {
}
