output_path             = "output"
output_size             = 1024 * 1024 * 1024
sandbox_load_path       = "load"
sandbox_run_path        = "run"
analysis_threads        = 0 -- default to sequential processing

input_defaults = {
    module_path         = "hindsight/io_modules",
    output_limit        = 1024 * 1024  * 8,
    instruction_limit   = 0,
    }

analysis_defaults = {
    module_path         = "hindsight/modules",
    output_limit        = 1024 * 64,        -- default
    memory_limit        = 1024 * 1024 * 8,  -- default
    instruction_limit   = 1000000,          -- default
    preserve_data       = false,            -- default
    ticker_interval     = 60,
    }

output_defaults = {
    module_path     = "hindsight/io_modules",
    output_limit    = 1024 * 1024  * 8,
    ticker_interval = 60,
    }

